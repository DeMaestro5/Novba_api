/**
 * Cache module placeholder.
 * App initializes this on startup; no Redis or cache implementation is used elsewhere yet.
 */
// No-op for now — avoids "Cannot find module './cache'" and allows server to start.
export {};
